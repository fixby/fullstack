---
name: retro
version: 2.0.0
description: |
  每周工程回顾。分析提交历史、工作模式和代码质量指标，支持持久化历史和趋势追踪。
  团队感知：分解每个人的贡献，包含表扬和成长建议。
  当被要求"每周回顾"、"我们发布了什么"或"工程回顾"时使用。
  在工作周或冲刺结束时主动建议使用。
allowed-tools:
  - Bash
  - Read
  - Write
  - Glob
  - AskUserQuestion
---

{{PREAMBLE}}

## 检测默认分支

在收集数据之前，检测仓库的默认分支名称：
`gh repo view --json defaultBranchRef -q .defaultBranchRef.name`

如果失败，则回退到 `main`。在下面的指令中，当提到 `origin/<default>` 时使用检测到的名称。

---

# /retro — 每周工程回顾

生成全面的工程回顾，分析提交历史、工作模式和代码质量指标。团队感知：识别运行命令的用户，然后分析每个贡献者，提供个人的表扬和成长机会。专为使用 Claude Code 作为力量倍增器的高级 IC/CTO 级别构建者设计。

## 用户可调用
当用户输入 `/retro` 时，运行此技能。

## 参数
- `/retro` — 默认：最近7天
- `/retro 24h` — 最近24小时
- `/retro 14d` — 最近14天
- `/retro 30d` — 最近30天
- `/retro compare` — 比较当前窗口与前一个相同长度窗口
- `/retro compare 14d` — 使用显式窗口进行比较
- `/retro global` — 跨所有 AI 编码工具的跨项目回顾（默认7天）
- `/retro global 14d` — 使用显式窗口的跨项目回顾

## 指令

解析参数以确定时间窗口。如果未提供参数，默认为7天。所有时间应以用户的**本地时区**报告（使用系统默认值 — 不要设置 `TZ`）。

**午夜对齐窗口：** 对于天（`d`）和周（`w`）单位，计算本地午夜的绝对开始日期，而不是相对字符串。例如，如果今天是 2026-03-18 且窗口为7天：开始日期为 2026-03-11。对 git log 查询使用 `--since="2026-03-11T00:00:00"` — 显式的 `T00:00:00` 后缀确保 git 从午夜开始。没有它，git 使用当前时钟时间（例如，在晚上11点时 `--since="2026-03-11"` 意味着晚上11点，而不是午夜）。对于周单位，乘以7得到天数（例如，`2w` = 14天前）。对于小时（`h`）单位，使用 `--since="N hours ago"`，因为午夜对齐不适用于子天窗口。

**参数验证：** 如果参数不匹配数字后跟 `d`、`h` 或 `w`，单词 `compare`（可选地后跟窗口），或单词 `global`（可选地后跟窗口），则显示此用法并停止：
```
Usage: /retro [window | compare | global]
  /retro              — last 7 days (default)
  /retro 24h          — last 24 hours
  /retro 14d          — last 14 days
  /retro 30d          — last 30 days
  /retro compare      — compare this period vs prior period
  /retro compare 14d  — compare with explicit window
  /retro global       — cross-project retro across all AI tools (7d default)
  /retro global 14d   — cross-project retro with explicit window
```

**如果第一个参数是 `global`：** 跳过正常的仓库范围回顾（步骤1-14）。相反，遵循本文档末尾的**全局回顾**流程。可选的第二个参数是时间窗口（默认7d）。此模式不需要在 git 仓库内。

### 步骤 1：收集原始数据

首先，获取 origin 并识别当前用户：
```bash
git fetch origin <default> --quiet
# 识别谁在运行回顾
git config user.name
git config user.email
```

`git config user.name` 返回的名称是**"你"** — 正在阅读此回顾的人。所有其他作者是队友。使用此来定位叙述："你的"提交 vs 队友贡献。

并行运行所有这些 git 命令（它们是独立的）：

```bash
# 1. 窗口内所有提交，包含时间戳、主题、哈希、作者、文件变更、插入、删除
git log origin/<default> --since="<window>" --format="%H|%aN|%ae|%ai|%s" --shortstat

# 2. 每个提交的测试 vs 总 LOC 分解，包含作者
#    每个提交块以 COMMIT:<hash>|<author> 开头，后跟 numstat 行。
#    将测试文件（匹配 test/|spec/|__tests__/）与生产文件分开。
git log origin/<default> --since="<window>" --format="COMMIT:%H|%aN" --numstat

# 3. 用于会话检测和小时分布的提交时间戳（包含作者）
git log origin/<default> --since="<window>" --format="%at|%aN|%ai|%s" | sort -n

# 4. 最频繁变更的文件（热点分析）
git log origin/<default> --since="<window>" --format="" --name-only | grep -v '^$' | sort | uniq -c | sort -rn

# 5. 从提交消息中提取 PR 编号（提取 #NNN 模式）
git log origin/<default> --since="<window>" --format="%s" | grep -oE '#[0-9]+' | sed 's/^#//' | sort -n | uniq | sed 's/^/#/'

# 6. 每个作者的文件热点（谁触碰了什么）
git log origin/<default> --since="<window>" --format="AUTHOR:%aN" --name-only

# 7. 每个作者的提交计数（快速摘要）
git shortlog origin/<default> --since="<window>" -sn --no-merges

# 8. Greptile 分类历史（如果可用）
cat ~/.openclaw/greptile-history.md 2>/dev/null || true

# 9. TODOS.md 待办事项（如果可用）
cat TODOS.md 2>/dev/null || true

# 10. 测试文件计数
find . -name '*.test.*' -o -name '*.spec.*' -o -name '*_test.*' -o -name '*_spec.*' 2>/dev/null | grep -v node_modules | wc -l

# 11. 窗口内的回归测试提交
git log origin/<default> --since="<window>" --oneline --grep="test(qa):" --grep="test(design):" --grep="test: coverage"

# 12. openclaw-skills 技能使用遥测（如果可用）
cat ~/.openclaw/analytics/skill-usage.jsonl 2>/dev/null || true

# 12. 窗口内变更的测试文件
git log origin/<default> --since="<window>" --format="" --name-only | grep -E '\.(test|spec)\.' | sort -u | wc -l
```

### 步骤 2：计算指标

计算并在摘要表中展示这些指标：

| 指标 | 值 |
|--------|-------|
| 提交到 main | N |
| 贡献者 | N |
| 合并的 PR | N |
| 总插入行数 | N |
| 总删除行数 | N |
| 净增 LOC | N |
| 测试 LOC（插入） | N |
| 测试 LOC 比率 | N% |
| 版本范围 | vX.Y.Z.W → vX.Y.Z.W |
| 活跃天数 | N |
| 检测到的会话 | N |
| 平均 LOC/会话小时 | N |
| Greptile 信号 | N%（Y 次捕获，Z 次误报） |
| 测试健康度 | N 个总测试 · M 个本周期新增 · K 个回归测试 |

然后立即在下方显示**每位作者的排行榜**：

```
贡献者              提交数   +/-          主要区域
You (garry)              32   +2400/-300   browse/
alice                    12   +800/-150    app/services/
bob                       3   +120/-40     tests/
```

按提交数降序排序。当前用户（来自 `git config user.name`）始终排在第一位，标记为"You (name)"。

**Greptile 信号（如果历史存在）：** 读取 `~/.openclaw/greptile-history.md`（在步骤1，命令8中获取）。按日期筛选回顾时间窗口内的条目。按类型计数条目：`fix`、`fp`、`already-fixed`。计算信号比率：`(fix + already-fixed) / (fix + already-fixed + fp)`。如果窗口内没有条目或文件不存在，跳过 Greptile 指标行。静默跳过无法解析的行。

**待办事项健康度（如果 TODOS.md 存在）：** 读取 `TODOS.md`（在步骤1，命令9中获取）。计算：
- 总开放 TODO（排除 `## Completed` 部分中的项目）
- P0/P1 计数（关键/紧急项目）
- P2 计数（重要项目）
- 本周期完成的项目（Completed 部分中日期在回顾窗口内的项目）
- 本周期新增的项目（交叉引用 git log 中在窗口内修改 TODOS.md 的提交）

包含在指标表中：
```
| 待办事项健康度 | N 个开放（X 个 P0/P1，Y 个 P2）· Z 个本周期完成 |
```

如果 TODOS.md 不存在，跳过待办事项健康度行。

**技能使用情况（如果分析数据存在）：** 如果存在 `~/.openclaw/analytics/skill-usage.jsonl`，则读取。按 `ts` 字段筛选回顾时间窗口内的条目。将技能激活（无 `event` 字段）与钩子触发（`event: "hook_fire"`）分开。按技能名称聚合。展示为：

```
| 技能使用 | /ship(12) /qa(8) /review(5) · 3 次安全钩子触发 |
```

如果 JSONL 文件不存在或窗口内没有条目，跳过技能使用行。

**顿悟时刻（如果已记录）：** 如果存在 `~/.openclaw/analytics/eureka.jsonl`，则读取。按 `ts` 字段筛选回顾时间窗口内的条目。对于每个顿悟时刻，显示标记它的技能、分支和洞察的一行摘要。展示为：

```
| 顿悟时刻 | 本周期 2 次 |
```

如果存在时刻，列出它们：
```
  EUREKA /office-hours (branch: garrytan/auth-rethink): "Session tokens don't need server storage — browser crypto API makes client-side JWT validation viable"
  EUREKA /plan-eng-review (branch: garrytan/cache-layer): "Redis isn't needed here — Bun's built-in LRU cache handles this workload"
```

如果 JSONL 文件不存在或窗口内没有条目，跳过顿悟时刻行。

### 步骤 3：提交时间分布

使用条形图显示本地时间的每小时直方图：

```
小时  提交数  ████████████████
 00:    4      ████
 07:    5      █████
 ...
```

识别并指出：
- 高峰时段
- 空白时段
- 模式是双峰（早晨/晚上）还是连续
- 深夜编码集群（晚上10点后）

### 步骤 4：工作会话检测

使用连续提交之间的**45分钟间隔**阈值检测会话。对于每个会话报告：
- 开始/结束时间（太平洋时间）
- 提交数量
- 持续时间（分钟）

分类会话：
- **深度会话**（50+ 分钟）
- **中等会话**（20-50 分钟）
- **微型会话**（<20 分钟，通常是单次提交的即兴操作）

计算：
- 总活跃编码时间（会话持续时间总和）
- 平均会话长度
- 每小时活跃时间的 LOC

### 步骤 5：提交类型分解

按约定式提交前缀分类（feat/fix/refactor/test/chore/docs）。显示为百分比条：

```
feat:     20  (40%)  ████████████████████
fix:      27  (54%)  ███████████████████████████
refactor:  2  ( 4%)  ██
```

如果修复比率超过50%则标记 — 这表明"快速发布，快速修复"模式，可能表示审查缺口。

### 步骤 6：热点分析

显示变更最多的前10个文件。标记：
- 变更5次以上的文件（流失热点）
- 热点列表中的测试文件 vs 生产文件
- VERSION/CHANGELOG 频率（版本纪律指标）

### 步骤 7：PR 大小分布

从提交差异中，估算 PR 大小并分组：
- **小型**（<100 LOC）
- **中型**（100-500 LOC）
- **大型**（500-1500 LOC）
- **超大型**（1500+ LOC）

### 步骤 8：专注度评分 + 本周之星

**专注度评分：** 计算触及单个最常变更的顶级目录的提交百分比（例如 `app/services/`、`app/views/`）。更高的分数 = 更深入的专注工作。更低的分数 = 分散的上下文切换。报告为："专注度评分：62%（app/services/）"

**本周之星：** 自动识别窗口内单个最高 LOC 的 PR。突出显示：
- PR 编号和标题
- LOC 变更
- 为什么重要（从提交消息和触及的文件推断）

### 步骤 9：团队成员分析

对于每个贡献者（包括当前用户），计算：

1. **提交和 LOC** — 总提交数、插入行数、删除行数、净 LOC
2. **专注领域** — 他们最常触及的目录/文件（前3个）
3. **提交类型组合** — 他们个人的 feat/fix/refactor/test 分解
4. **会话模式** — 他们何时编码（高峰时段）、会话计数
5. **测试纪律** — 他们个人的测试 LOC 比率
6. **最大贡献** — 他们窗口内单个最高影响的提交或 PR

**对于当前用户（"You"）：** 此部分获得最深入的处理。包含个人回顾的所有细节 — 会话分析、时间模式、专注度评分。以第一人称表述："你的高峰时段..."、"你的最大贡献..."

**对于每个队友：** 写2-3句话涵盖他们的工作和模式。然后：

- **表扬**（1-2个具体事项）：基于实际提交。不是"干得好" — 而是确切说出哪里好。示例："在3个专注会话中完成了整个认证中间件重写，测试覆盖率45%"、"每个 PR 都在200 LOC 以下 — 有纪律的分解。"
- **成长机会**（1个具体事项）：框架为升级建议，而非批评。基于实际数据。示例："本周测试比率为12% — 在支付模块变得更复杂之前添加测试覆盖率会有回报"、"同一文件上的5个修复提交表明原始 PR 可能需要一次审查。"

**如果只有一个贡献者（个人仓库）：** 跳过团队分解并按之前进行 — 回顾是个人的。

**如果有 Co-Authored-By 标记：** 解析提交消息中的 `Co-Authored-By:` 行。与主要作者一起为这些作者记入提交。注意 AI 共同作者（例如 `noreply@anthropic.com`）但不要将他们作为团队成员 — 相反，将"AI 辅助提交"作为单独的指标跟踪。

### 步骤 10：周对周趋势（如果窗口 >= 14d）

如果时间窗口为14天或更长，分为每周桶并显示趋势：
- 每周提交（总计和每位作者）
- 每周 LOC
- 每周测试比率
- 每周修复比率
- 每周会话计数

### 步骤 11：连续性追踪

从今天开始，计算连续至少有1次提交到 origin/<default> 的天数。跟踪团队连续性和个人连续性：

```bash
# 团队连续性：所有唯一提交日期（本地时间）— 无硬性截止
git log origin/<default> --format="%ad" --date=format:"%Y-%m-%d" | sort -u

# 个人连续性：仅当前用户的提交
git log origin/<default> --author="<user_name>" --format="%ad" --date=format:"%Y-%m-%d" | sort -u
```

从今天开始倒计数 — 有多少连续天数至少有一次提交？这查询完整历史，因此可以准确报告任何长度的连续性。显示两者：
- "团队发布连续性：47个连续天"
- "你的发布连续性：32个连续天"

### 步骤 12：加载历史并比较

在保存新快照之前，检查之前的回顾历史：

```bash
ls -t .context/retros/*.json 2>/dev/null
```

**如果存在之前的回顾：** 使用 Read 工具加载最近的一个。计算关键指标的增量并包含**与上次回顾的趋势**部分：
```
                    上次        现在         增量
测试比率:         22%    →    41%         ↑19pp
会话:           10     →    14          ↑4
LOC/小时:           200    →    350         ↑75%
修复比率:          54%    →    30%         ↓24pp（改善中）
提交:            32     →    47          ↑47%
深度会话:      3      →    5           ↑2
```

**如果不存在之前的回顾：** 跳过比较部分并附加："首次记录回顾 — 下周再次运行以查看趋势。"

### 步骤 13：保存回顾历史

在计算所有指标（包括连续性）并加载任何之前的历史进行比较后，保存 JSON 快照：

```bash
mkdir -p .context/retros
```

确定今天的下一个序列号（将 `$(date +%Y-%m-%d)` 替换为实际日期）：
```bash
# 计算今天现有的回顾数以获取下一个序列号
today=$(date +%Y-%m-%d)
existing=$(ls .context/retros/${today}-*.json 2>/dev/null | wc -l | tr -d ' ')
next=$((existing + 1))
# 保存为 .context/retros/${today}-${next}.json
```

使用 Write 工具保存具有此模式的 JSON 文件：
```json
{
  "date": "2026-03-08",
  "window": "7d",
  "metrics": {
    "commits": 47,
    "contributors": 3,
    "prs_merged": 12,
    "insertions": 3200,
    "deletions": 800,
    "net_loc": 2400,
    "test_loc": 1300,
    "test_ratio": 0.41,
    "active_days": 6,
    "sessions": 14,
    "deep_sessions": 5,
    "avg_session_minutes": 42,
    "loc_per_session_hour": 350,
    "feat_pct": 0.40,
    "fix_pct": 0.30,
    "peak_hour": 22,
    "ai_assisted_commits": 32
  },
  "authors": {
    "Garry Tan": { "commits": 32, "insertions": 2400, "deletions": 300, "test_ratio": 0.41, "top_area": "browse/" },
    "Alice": { "commits": 12, "insertions": 800, "deletions": 150, "test_ratio": 0.35, "top_area": "app/services/" }
  },
  "version_range": ["1.16.0.0", "1.16.1.0"],
  "streak_days": 47,
  "tweetable": "Week of Mar 1: 47 commits (3 contributors), 3.2k LOC, 38% tests, 12 PRs, peak: 10pm",
  "greptile": {
    "fixes": 3,
    "fps": 1,
    "already_fixed": 2,
    "signal_pct": 83
  }
}
```

**注意：** 仅当 `~/.openclaw/greptile-history.md` 存在且时间窗口内有条目时，才包含 `greptile` 字段。仅当 `TODOS.md` 存在时，才包含 `backlog` 字段。仅当找到测试文件（命令10返回 > 0）时，才包含 `test_health` 字段。如果任何一项没有数据，则完全省略该字段。

当测试文件存在时，在 JSON 中包含测试健康数据：
```json
  "test_health": {
    "total_test_files": 47,
    "tests_added_this_period": 5,
    "regression_test_commits": 3,
    "test_files_changed": 8
  }
```

当 TODOS.md 存在时，在 JSON 中包含待办事项数据：
```json
  "backlog": {
    "total_open": 28,
    "p0_p1": 2,
    "p2": 8,
    "completed_this_period": 3,
    "added_this_period": 1
  }
```

### 步骤 14：撰写叙述

将输出结构化为：

---

**可分享摘要**（第一行，在所有内容之前）：
```
Week of Mar 1: 47 commits (3 contributors), 3.2k LOC, 38% tests, 12 PRs, peak: 10pm | Streak: 47d
```

## 工程回顾：[日期范围]

### 摘要表
（来自步骤2）

### 与上次回顾的趋势
（来自步骤11，在保存前加载 — 如果是首次回顾则跳过）

### 时间和会话模式
（来自步骤3-4）

解释团队范围模式含义的叙述：
- 最有成效的小时是什么时候，以及驱动它们的因素
- 会话是随时间变长还是变短
- 每天活跃编码的估计小时数（团队总计）
- 值得注意的模式：团队成员是同时编码还是轮班？

### 发布速度
（来自步骤5-7）

涵盖以下内容的叙述：
- 提交类型组合及其揭示的内容
- PR 大小分布及其揭示的发布节奏
- 修复链检测（同一子系统上的修复提交序列）
- 版本号更新纪律

### 代码质量信号
- 测试 LOC 比率趋势
- 热点分析（相同文件是否在流失？）
- Greptile 信号比率和趋势（如果历史存在）："Greptile：X% 信号（Y 次有效捕获，Z 次误报）"

### 测试健康度
- 总测试文件：N（来自命令10）
- 本周期新增测试：M（来自命令12 — 变更的测试文件）
- 回归测试提交：列出命令11中的 `test(qa):` 和 `test(design):` 和 `test: coverage` 提交
- 如果存在之前的回顾且有 `test_health`：显示增量"测试计数：{上次} → {现在}（+{增量}）"
- 如果测试比率 < 20%：标记为成长领域 — "100% 测试覆盖率是目标。测试让氛围编码变得安全。"

### 专注度和亮点
（来自步骤8）
- 专注度评分及其解释
- 本周之星亮点

### 你的一周（个人深度分析）
（来自步骤9，仅针对当前用户）

这是用户最关心的部分。包括：
- 他们的个人提交计数、LOC、测试比率
- 他们的会话模式和高峰时段
- 他们的专注领域
- 他们的最大贡献
- **你做得好的地方**（2-3个基于提交的具体事项）
- **需要提升的地方**（1-2个具体、可操作的建议）

### 团队分解
（来自步骤9，针对每个队友 — 如果是个人仓库则跳过）

对于每个队友（按提交数降序排序），写一个部分：

#### [姓名]
- **他们的贡献**：2-3句话描述他们的贡献、专注领域和提交模式
- **表扬**：1-2个他们做得好的具体事项，基于实际提交。要真诚 — 你在1对1中实际会说什么？示例：
  - "在3个小型、可审查的 PR 中清理了整个认证模块 — 教科书式的分解"
  - "为每个新端点添加了集成测试，而不仅仅是快乐路径"
  - "修复了导致仪表板2秒加载时间的 N+1 查询"
- **成长机会**：1个具体、建设性的建议。框架为投资，而非批评。示例：
  - "支付模块的测试覆盖率为8% — 在下一个功能叠加之前值得投资"
  - "大多数提交都在单个突发中 — 将工作分散到一天中可以减少上下文切换疲劳"
  - "所有提交都在凌晨1-4点之间 — 可持续的节奏对长期代码质量很重要"

**AI 协作说明：** 如果许多提交有 `Co-Authored-By` AI 标记（例如 Claude、Copilot），将 AI 辅助提交百分比作为团队指标记录。中立地表述 — "N% 的提交是 AI 辅助的" — 不加评判。

### 团队前3大胜利
识别窗口内整个团队发布的3个最高影响的事项。对于每个：
- 它是什么
- 谁发布的
- 为什么重要（产品/架构影响）

### 3个需要改进的事项
具体、可操作、基于实际提交。混合个人和团队层面的建议。表述为"为了变得更好，团队可以..."

### 下周的3个习惯
小型、实用、现实。每个都必须是可以在5分钟内采用的事项。至少一个应该是团队导向的（例如，"当天审查彼此的 PR"）。

### 周对周趋势
（如果适用，来自步骤10）

---

## 全局回顾模式

当用户运行 `/retro global`（或 `/retro global 14d`）时，遵循此流程而不是仓库范围的步骤1-14。此模式可从任何目录工作 — 不需要在 git 仓库内。

### 全局步骤 1：计算时间窗口

与常规回顾相同的午夜对齐逻辑。默认7d。`global` 之后的第二个参数是窗口（例如 `14d`、`30d`、`24h`）。

### 全局步骤 2：运行发现

使用此回退链定位并运行发现脚本：

```bash
DISCOVER_BIN=""
[ -x ~/.claude/skills/openclaw-skills/bin/openclaw-global-discover ] && DISCOVER_BIN=~/.claude/skills/openclaw-skills/bin/openclaw-global-discover
[ -z "$DISCOVER_BIN" ] && [ -x .claude/skills/openclaw-skills/bin/openclaw-global-discover ] && DISCOVER_BIN=.claude/skills/openclaw-skills/bin/openclaw-global-discover
[ -z "$DISCOVER_BIN" ] && which openclaw-global-discover >/dev/null 2>&1 && DISCOVER_BIN=$(which openclaw-global-discover)
[ -z "$DISCOVER_BIN" ] && [ -f bin/openclaw-global-discover.ts ] && DISCOVER_BIN="bun run bin/openclaw-global-discover.ts"
echo "DISCOVER_BIN: $DISCOVER_BIN"
```

如果未找到二进制文件，告诉用户："未找到发现脚本。在 openclaw-skills 目录中运行 `bun run build` 来编译它。"并停止。

运行发现：
```bash
$DISCOVER_BIN --since "<window>" --format json 2>/tmp/openclaw-discover-stderr
```

从 `/tmp/openclaw-discover-stderr` 读取 stderr 输出以获取诊断信息。从 stdout 解析 JSON 输出。

如果 `total_sessions` 为0，说："在过去 <window> 内未找到 AI 编码会话。尝试更长的窗口：`/retro global 30d`"并停止。

### 全局步骤 3：在每个发现的仓库上运行 git log

对于发现 JSON 的 `repos` 数组中的每个仓库，在 `paths[]` 中找到第一个有效路径（目录存在且有 `.git/`）。如果不存在有效路径，跳过该仓库并记录。

**对于仅本地的仓库**（`remote` 以 `local:` 开头）：跳过 `git fetch` 并使用本地默认分支。使用 `git log HEAD` 而不是 `git log origin/$DEFAULT`。

**对于有远程的仓库：**

```bash
git -C <path> fetch origin --quiet 2>/dev/null
```

检测每个仓库的默认分支：首先尝试 `git symbolic-ref refs/remotes/origin/HEAD`，然后检查常见分支名称（`main`、`master`），然后回退到 `git rev-parse --abbrev-ref HEAD`。在下面的命令中使用检测到的分支作为 `<default>`。

```bash
# 带统计的提交
git -C <path> log origin/$DEFAULT --since="<start_date>T00:00:00" --format="%H|%aN|%ai|%s" --shortstat

# 用于会话检测、连续性和上下文切换的提交时间戳
git -C <path> log origin/$DEFAULT --since="<start_date>T00:00:00" --format="%at|%aN|%ai|%s" | sort -n

# 每个作者的提交计数
git -C <path> shortlog origin/$DEFAULT --since="<start_date>T00:00:00" -sn --no-merges

# 从提交消息中提取 PR 编号
git -C <path> log origin/$DEFAULT --since="<start_date>T00:00:00" --format="%s" | grep -oE '#[0-9]+' | sort -n | uniq
```

对于失败的仓库（删除的路径、网络错误）：跳过并记录"N 个仓库无法访问。"

### 全局步骤 4：计算全局发布连续性

对于每个仓库，获取提交日期（上限365天）：

```bash
git -C <path> log origin/$DEFAULT --since="365 days ago" --format="%ad" --date=format:"%Y-%m-%d" | sort -u
```

合并所有仓库的所有日期。从今天开始倒计数 — 有多少连续天数至少有一次提交到任何仓库？如果连续性达到365天，显示为"365+ 天"。

### 全局步骤 5：计算上下文切换指标

从步骤3收集的提交时间戳中，按日期分组。对于每个日期，计算当天有多少个不同的仓库有提交。报告：
- 平均仓库数/天
- 最大仓库数/天
- 哪些天是专注的（1个仓库）vs. 碎片化的（3+个仓库）

### 全局步骤 6：每个工具的生产力模式

从发现 JSON 中，分析工具使用模式：
- 哪个 AI 工具用于哪些仓库（独占 vs. 共享）
- 每个工具的会话计数
- 行为模式（例如，"Codex 专门用于 myapp，Claude Code 用于其他所有内容"）

### 全局步骤 7：聚合并生成叙述

将输出结构化为**可分享的个人卡片优先**，然后是完整的团队/项目分解。个人卡片设计为截图友好 — 人们想在 X/Twitter 上分享的所有内容都在一个干净的块中。

---

**可分享摘要**（第一行，在所有内容之前）：
```
Week of Mar 14: 5 projects, 138 commits, 250k LOC across 5 repos | 48 AI sessions | Streak: 52d 🔥
```

## 🚀 你的一周：[用户名] — [日期范围]

此部分是**可分享的个人卡片**。它仅包含当前用户的统计数据 — 没有团队数据，没有项目分解。设计为截图和发布。

使用 `git config user.name` 的用户身份筛选所有每个仓库的 git 数据。跨所有仓库聚合以计算个人总计。

渲染为单个视觉干净的块。仅左边框 — 无右边框（LLM 无法可靠地对齐右边框）。将仓库名称填充到最长的名称，以便列整齐对齐。永远不要截断项目名称。

```
╔═══════════════════════════════════════════════════════════════
║  [用户名] — [日期]的一周
╠═══════════════════════════════════════════════════════════════
║
║  [N] 次提交跨 [M] 个项目
║  +[X]k LOC 添加 · [Y]k LOC 删除 · [Z]k 净增
║  [N] 个 AI 编码会话（CC: X, Codex: Y, Gemini: Z）
║  [N] 天发布连续性 🔥
║
║  项目
║  ─────────────────────────────────────────────────────────
║  [完整仓库名]        [N] 次提交    +[X]k LOC    [个人/团队]
║  [完整仓库名]        [N] 次提交    +[X]k LOC    [个人/团队]
║  [完整仓库名]        [N] 次提交    +[X]k LOC    [个人/团队]
║
║  本周之星
║  [PR 标题] — [LOC] 行跨 [N] 个文件
║
║  顶级工作
║  • [最大主题的一行描述]
║  • [第二大主题的一行描述]
║  • [第三大主题的一行描述]
║
║  Powered by OpenClaw Skills
╚═══════════════════════════════════════════════════════════════
```

**个人卡片规则：**
- 仅显示用户有提交的仓库。跳过0提交的仓库。
- 按用户的提交数降序排序仓库。
- **永远不要截断仓库名称。** 使用完整的仓库名称（例如 `analyze_transcripts` 而不是 `analyze_trans`）。将名称列填充到最长的仓库名称，以便所有列整齐对齐。如果名称很长，加宽框 — 框宽度适应内容。
- 对于 LOC，使用"k"格式表示千（例如 "+64.0k" 而不是 "+64010"）。
- 角色：如果用户是唯一贡献者则为"个人"，如果其他人贡献则为"团队"。
- 本周之星：用户跨所有仓库的单个最高 LOC 的 PR。
- 顶级工作：3个要点总结用户的主要主题，从提交消息推断。不是单个提交 — 综合成主题。例如，"构建了 /retro global — 跨项目回顾与 AI 会话发现"而不是"feat: openclaw-global-discover" + "feat: /retro global template"。
- 卡片必须是自包含的。仅看到此块的人应该能够在没有任何周围上下文的情况下理解用户的一周。
- 不要在此包含团队成员、项目总计或上下文切换数据。

**个人连续性：** 使用用户自己在所有仓库中的提交（通过 `--author` 筛选）来计算个人连续性，与团队连续性分开。

---

## 全局工程回顾：[日期范围]

下面的所有内容是完整分析 — 团队数据、项目分解、模式。这是可分享卡片之后的"深度分析"。

### 所有项目概览
| 指标 | 值 |
|--------|-------|
| 活跃项目数 | N |
| 总提交数（所有仓库，所有贡献者） | N |
| 总 LOC | +N / -N |
| AI 编码会话 | N（CC: X, Codex: Y, Gemini: Z） |
| 活跃天数 | N |
| 全局发布连续性（任何贡献者，任何仓库） | N 个连续天 |
| 上下文切换/天 | N 平均（最大：M） |

### 每个项目分解
对于每个仓库（按提交数降序排序）：
- 仓库名称（占总提交数的百分比）
- 提交数、LOC、合并的 PR、顶级贡献者
- 主要工作（从提交消息推断）
- 按工具的 AI 会话

**你的贡献**（每个项目内的子部分）：
对于每个项目，添加一个"你的贡献"块，显示当前用户在该仓库内的个人统计数据。使用 `git config user.name` 的用户身份进行筛选。包括：
- 你的提交数 / 总提交数（带百分比）
- 你的 LOC（+插入 / -删除）
- 你的主要工作（仅从你的提交消息推断）
- 你的提交类型组合（feat/fix/refactor/chore/docs 分解）
- 你在此仓库中的最大发布（最高 LOC 的提交或 PR）

如果用户是唯一贡献者，说"个人项目 — 所有提交都是你的。"
如果用户在仓库中有 0 次提交（本周期未触碰的团队项目），说"本周期无提交 — 仅 [N] 次 AI 会话。"并跳过分解。

格式：
```
**你的贡献：** 47/244 次提交（19%），+4.2k/-0.3k LOC
  主要工作：Writer Chat，邮件屏蔽，安全加固
  最大发布：PR #605 — Writer Chat 占据管理栏（2,457 行插入，46 个文件）
  组合：feat(3) fix(2) chore(1)
```

### 跨项目模式
- 跨项目的时间分配（百分比分解，使用你的提交而非总计）
- 跨所有仓库聚合的高峰生产力时段
- 专注 vs. 碎片化的天数
- 上下文切换趋势

### 工具使用分析
按工具分解及行为模式：
- Claude Code：跨 M 个仓库的 N 次会话 — 观察到的模式
- Codex：跨 M 个仓库的 N 次会话 — 观察到的模式
- Gemini：跨 M 个仓库的 N 次会话 — 观察到的模式

### 本周之星（全局）
跨所有项目的最高影响 PR。通过 LOC 和提交消息识别。

### 3 个跨项目洞察
全局视图揭示的任何单一仓库回顾无法展示的内容。

### 下周的 3 个习惯
考虑完整的跨项目情况。

---

### 全局步骤 8：加载历史并比较

```bash
ls -t ~/.openclaw/retros/global-*.json 2>/dev/null | head -5
```

**仅与具有相同 `window` 值的之前回顾进行比较**（例如，7d vs 7d）。如果最近的之前回顾有不同的窗口，跳过比较并注明："之前的全局回顾使用了不同的窗口 — 跳过比较。"

如果存在匹配的之前回顾，使用 Read 工具加载它。显示**与上次全局回顾的趋势**表，包含关键指标的增量：总提交数、LOC、会话、连续性、上下文切换/天。

如果不存在之前的全局回顾，附加："首次记录全局回顾 — 下周再次运行以查看趋势。"

### 全局步骤 9：保存快照

```bash
mkdir -p ~/.openclaw/retros
```

确定今天的下一个序列号：
```bash
today=$(date +%Y-%m-%d)
existing=$(ls ~/.openclaw/retros/global-${today}-*.json 2>/dev/null | wc -l | tr -d ' ')
next=$((existing + 1))
```

使用 Write 工具将 JSON 保存到 `~/.openclaw/retros/global-${today}-${next}.json`：

```json
{
  "type": "global",
  "date": "2026-03-21",
  "window": "7d",
  "projects": [
    {
      "name": "openclaw-skills",
      "remote": "https://github.com/openclaw/openclaw-skills",
      "commits": 47,
      "insertions": 3200,
      "deletions": 800,
      "sessions": { "claude_code": 15, "codex": 3, "gemini": 0 }
    }
  ],
  "totals": {
    "commits": 182,
    "insertions": 15300,
    "deletions": 4200,
    "projects": 5,
    "active_days": 6,
    "sessions": { "claude_code": 48, "codex": 8, "gemini": 3 },
    "global_streak_days": 52,
    "avg_context_switches_per_day": 2.1
  },
  "tweetable": "Week of Mar 14: 5 projects, 182 commits, 15.3k LOC | CC: 48, Codex: 8, Gemini: 3 | Focus: openclaw-skills (58%) | Streak: 52d"
}
```

---

## 比较模式

当用户运行 `/retro compare`（或 `/retro compare 14d`）时：

1. 使用午夜对齐的开始日期计算当前窗口的指标（默认 7d）（与主回顾相同的逻辑 — 例如，如果今天是 2026-03-18 且窗口为 7d，使用 `--since="2026-03-11T00:00:00"`）
2. 使用 `--since` 和 `--until` 及午夜对齐的日期计算紧邻的前一个相同长度窗口的指标以避免重叠（例如，对于从 2026-03-11 开始的 7d 窗口：前一个窗口是 `--since="2026-03-04T00:00:00" --until="2026-03-11T00:00:00"`）
3. 显示带有增量和箭头的并排比较表
4. 撰写简短的叙述，突出最大的改进和退步
5. 仅将当前窗口的快照保存到 `.context/retros/`（与正常回顾运行相同）；**不要**持久化前一个窗口的指标。

## 语气

- 鼓励但坦诚，不哄骗
- 具体而实在 — 始终锚定在实际提交/代码中
- 跳过泛泛的表扬（"干得好！"）— 准确说出哪里好以及为什么
- 将改进框架为升级，而非批评
- **表扬应该感觉像你在 1:1 中实际会说的** — 具体、应得、真诚
- **成长建议应该感觉像投资建议** — "这值得你花时间，因为..."而不是"你在...失败了"
- 永远不要负面地比较队友。每个人的部分独立存在。
- 保持总输出在 3000-4500 字左右（稍长以适应团队部分）
- 使用 markdown 表格和代码块展示数据，用散文叙述
- 直接输出到对话 — 不要写入文件系统（除了 `.context/retros/` JSON 快照）

## 重要规则

- 所有叙述输出直接发送给对话中的用户。唯一写入的文件是 `.context/retros/` JSON 快照。
- 对所有 git 查询使用 `origin/<default>`（不是可能过时的本地 main）
- 以用户的本地时区显示所有时间戳（不要覆盖 `TZ`）
- 如果窗口有零提交，说明并建议不同的窗口
- 将 LOC/小时四舍五入到最近的 50
- 将合并提交视为 PR 边界
- 不要读取 CLAUDE.md 或其他文档 — 此技能是自包含的
- 首次运行（无之前的回顾）时，优雅地跳过比较部分
- **全局模式：** 不需要在 git 仓库内。将快照保存到 `~/.openclaw/retros/`（不是 `.context/retros/`）。优雅地跳过未安装的 AI 工具。仅与具有相同窗口值的之前全局回顾进行比较。如果连续性达到 365d 上限，显示为"365+ 天"。
