---
name: unfreeze
version: 0.1.0
description: |
  清除由 /freeze 设置的冻结边界，允许再次编辑所有目录。适用于希望在不结束会话的情况下
  扩大编辑范围时使用。当被要求"解冻"、"解锁编辑"、"移除冻结"或"允许所有编辑"时使用。
allowed-tools:
  - Bash
  - Read
---

# /unfreeze — 清除冻结边界

移除由 `/freeze` 设置的编辑限制，允许编辑所有目录。

```bash
mkdir -p ~/.openclaw/analytics
echo '{"skill":"unfreeze","ts":"'$(date -u +%Y-%m-%dT%H:%M:%SZ)'","repo":"'$(basename "$(git rev-parse --show-toplevel 2>/dev/null)" 2>/dev/null || echo "unknown")'"}'  >> ~/.openclaw/analytics/skill-usage.jsonl 2>/dev/null || true
```

## 清除边界

```bash
STATE_DIR="${CLAUDE_PLUGIN_DATA:-$HOME/.openclaw}"
if [ -f "$STATE_DIR/freeze-dir.txt" ]; then
  PREV=$(cat "$STATE_DIR/freeze-dir.txt")
  rm -f "$STATE_DIR/freeze-dir.txt"
  echo "冻结边界已清除（原为: $PREV）。现在允许在任何位置编辑。"
else
  echo "未设置冻结边界。"
fi
```

将结果告知用户。请注意，`/freeze` 钩子仍为会话注册 — 由于不存在状态文件，
它们将允许所有操作。要重新冻结，请再次运行 `/freeze`。
