---
name: setup-browser-cookies
version: 1.0.0
description: |
  从你的真实浏览器（Comet、Chrome、Arc、Brave、Edge）导入 cookies 到
  无头浏览会话。打开交互式选择器 UI，让你选择要导入哪些 cookie 域。
  在 QA 测试认证页面之前使用。当被要求"导入 cookies"、"登录网站"或
  "认证浏览器"时使用。
allowed-tools:
  - Bash
  - Read
  - AskUserQuestion
---

{{PREAMBLE}}

# 设置浏览器 Cookies

从你的真实 Chromium 浏览器导入已登录的会话到无头浏览会话。

## 工作原理

1. 查找 browse 二进制文件
2. 运行 `cookie-import-browser` 检测已安装的浏览器并打开选择器 UI
3. 用户在浏览器中选择要导入的 cookie 域
4. Cookies 被解密并加载到 Playwright 会话中

## 步骤

### 1. 查找 browse 二进制文件

{{BROWSE_SETUP}}

### 2. 打开 cookie 选择器

```bash
$B cookie-import-browser
```

这会自动检测已安装的 Chromium 浏览器（Comet、Chrome、Arc、Brave、Edge）并在你的默认浏览器中打开交互式选择器 UI，你可以在其中：
- 在已安装的浏览器之间切换
- 搜索域名
- 点击"+"导入某个域名的 cookies
- 点击垃圾桶删除已导入的 cookies

告诉用户：**"Cookie 选择器已打开 — 在浏览器中选择你想导入的域名，然后告诉我完成了。"**

### 3. 直接导入（替代方案）

如果用户直接指定了域名（例如 `/setup-browser-cookies github.com`），跳过 UI：

```bash
$B cookie-import-browser comet --domain github.com
```

如果指定了浏览器，将 `comet` 替换为相应的浏览器。

### 4. 验证

在用户确认完成后：

```bash
$B cookies
```

向用户展示已导入 cookies 的摘要（域名计数）。

## 注意事项

- 每个浏览器的首次导入可能会触发 macOS 钥匙串对话框 — 点击"允许"/"始终允许"
- Cookie 选择器在与 browse 服务器相同的端口上提供（无额外进程）
- UI 中仅显示域名和 cookie 计数 — 不暴露 cookie 值
- Browse 会话在命令之间持久化 cookies，因此导入的 cookies 立即生效
