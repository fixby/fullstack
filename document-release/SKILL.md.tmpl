---
name: document-release
version: 1.0.0
description: |
  发布后文档更新。读取所有项目文档，交叉引用差异，更新 README/ARCHITECTURE/CONTRIBUTING/CLAUDE.md 以匹配已发布内容，润色 CHANGELOG 措辞，清理 TODOS，并可选地更新 VERSION。当被要求"更新文档"、"同步文档"或"发布后文档"时使用。
  在 PR 合并或代码发布后主动建议。
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Grep
  - Glob
  - AskUserQuestion
---

{{PREAMBLE}}

{{BASE_BRANCH_DETECT}}

# 文档发布：发布后文档更新

你正在运行 `/document-release` 工作流。这在 `/ship` 之后（代码已提交，PR 存在或即将存在）但在 **PR 合并之前**运行。你的工作：确保项目中的每个文档文件准确、最新，并以友好、面向用户的口吻编写。

你主要是自动化的。直接进行明显的事实更新。仅在风险或主观决策时停止并询问。

**仅在以下情况停止：**
- 风险/有问题的文档更改（叙述性、理念、安全性、删除、大型重写）
- VERSION 升级决策（如果尚未升级）
- 要添加的新 TODOS 项目
- 文档间的叙述性矛盾（非事实性）

**永不停止：**
- 明显来自差异的事实更正
- 向表格/列表添加项目
- 更新路径、计数、版本号
- 修复过时的交叉引用
- CHANGELOG 措辞润色（微小的措辞调整）
- 标记 TODOS 完成
- 文档间的事实不一致（例如，版本号不匹配）

**永不执行：**
- 覆盖、替换或重新生成 CHANGELOG 条目 — 仅润色措辞，保留所有内容
- 不询问就升级 VERSION — 始终使用 AskUserQuestion 进行版本更改
- 对 CHANGELOG.md 使用 `Write` 工具 — 始终使用带有精确 `old_string` 匹配的 `Edit`

---

## 步骤 1：预检与差异分析

1. 检查当前分支。如果在基础分支上，**中止**："你在基础分支上。请从功能分支运行。"

2. 收集关于更改内容的上下文：

```bash
git diff <base>...HEAD --stat
```

```bash
git log <base>..HEAD --oneline
```

```bash
git diff <base>...HEAD --name-only
```

3. 发现仓库中的所有文档文件：

```bash
find . -maxdepth 2 -name "*.md" -not -path "./.git/*" -not -path "./node_modules/*" -not -path "./.openclaw/*" -not -path "./.context/*" | sort
```

4. 将更改分类为与文档相关的类别：
   - **新功能** — 新文件、新命令、新技能、新能力
   - **行为变更** — 修改的服务、更新的 API、配置更改
   - **移除的功能** — 删除的文件、移除的命令
   - **基础设施** — 构建系统、测试基础设施、CI

5. 输出简要摘要："正在分析 M 次提交中更改的 N 个文件。发现 K 个文档文件需要审查。"

---

## 步骤 2：逐文件文档审计

读取每个文档文件并将其与差异交叉引用。使用这些通用启发式方法（适应你所在的任何项目 — 这些不是 openclaw-skills 特定的）：

**README.md：**
- 它是否描述了差异中可见的所有功能和能力？
- 安装/设置说明是否与更改一致？
- 示例、演示和用法描述是否仍然有效？
- 故障排除步骤是否仍然准确？

**ARCHITECTURE.md：**
- ASCII 图表和组件描述是否与当前代码匹配？
- 设计决策和"为什么"解释是否仍然准确？
- 要保守 — 只更新差异明确矛盾的内容。架构文档
  描述不太可能频繁更改的内容。

**CONTRIBUTING.md — 新贡献者冒烟测试：**
- 像全新贡献者一样逐步执行设置说明。
- 列出的命令是否准确？每一步都会成功吗？
- 测试层级描述是否与当前测试基础设施匹配？
- 工作流描述（开发设置、贡献者模式等）是否是最新的？
- 标记任何会失败或让首次贡献者困惑的内容。

**CLAUDE.md / 项目说明：**
- 项目结构部分是否与实际文件树匹配？
- 列出的命令和脚本是否准确？
- 构建/测试说明是否与 package.json（或等效文件）中的内容匹配？

**任何其他 .md 文件：**
- 读取文件，确定其目的和受众。
- 与差异交叉引用以检查它是否与文件所说的内容矛盾。

对于每个文件，将所需更新分类为：

- **自动更新** — 差异明确保证的事实更正：向表格添加项目、更新文件路径、修复计数、更新项目结构树。
- **询问用户** — 叙述性更改、部分删除、安全模型更改、大型重写（一个部分超过约 10 行）、相关性模糊、添加全新部分。

---

## 步骤 3：应用自动更新

使用 Edit 工具直接进行所有明确的事实更新。

对于每个修改的文件，输出一行摘要描述**具体更改了什么** — 不只是"更新了 README.md"，而是"README.md：向技能表添加了 /new-skill，将技能计数从 9 更新为 10。"

**永不自动更新：**
- README 介绍或项目定位
- ARCHITECTURE 理念或设计原理
- 安全模型描述
- 不要从任何文档中删除整个部分

---

## 步骤 4：询问风险/有问题的更改

对于步骤 2 中识别的每个风险或有问题的更新，使用 AskUserQuestion：
- 上下文：项目名称、分支、哪个文档文件、我们在审查什么
- 具体的文档决策
- `建议：选择 [X] 因为 [一行原因]`
- 选项包括 C) 跳过 — 保持原样

每个回答后立即应用批准的更改。

---

## 步骤 5：CHANGELOG 措辞润色

**关键 — 永不覆盖 CHANGELOG 条目。**

此步骤润色措辞。它不会重写、替换或重新生成 CHANGELOG 内容。

曾发生过一个真实事件，代理在应该保留现有 CHANGELOG 条目时替换了它们。此技能必须永不这样做。

**规则：**
1. 首先读取整个 CHANGELOG.md。了解已有内容。
2. 仅修改现有条目内的措辞。永不删除、重新排序或替换条目。
3. 永不从零重新生成 CHANGELOG 条目。条目是由 `/ship` 从实际差异和提交历史编写的。它是事实来源。你在润色文字，不是重写历史。
4. 如果条目看起来错误或不完整，使用 AskUserQuestion — 不要静默修复。
5. 使用带有精确 `old_string` 匹配的 Edit 工具 — 永不使用 Write 覆盖 CHANGELOG.md。

**如果 CHANGELOG 在此分支中未被修改：** 跳过此步骤。

**如果 CHANGELOG 在此分支中被修改**，审查条目的措辞：

- **推销测试：** 用户阅读每个要点时会想"哦不错，我想试试那个"吗？如果不会，重写措辞（不是内容）。
- 以用户现在可以**做什么**开头 — 不是实现细节。
- "你现在可以..."而不是"重构了..."
- 标记并重写任何读起来像提交消息的条目。
- 内部/贡献者更改属于单独的"### 贡献者"子部分。
- 自动修复微小的措辞调整。如果重写会改变含义，使用 AskUserQuestion。

---

## 步骤 6：跨文档一致性与可发现性检查

单独审计每个文件后，进行跨文档一致性检查：

1. README 的功能/能力列表是否与 CLAUDE.md（或项目说明）描述的匹配？
2. ARCHITECTURE 的组件列表是否与 CONTRIBUTING 的项目结构描述匹配？
3. CHANGELOG 的最新版本是否与 VERSION 文件匹配？
4. **可发现性：** 每个文档文件是否可以从 README.md 或 CLAUDE.md 访问？如果 ARCHITECTURE.md 存在但 README 和 CLAUDE.md 都没有链接到它，标记它。每个文档应该可以从两个入口文件之一发现。
5. 标记文档间的任何矛盾。自动修复明确的事实不一致（例如，版本不匹配）。对叙述性矛盾使用 AskUserQuestion。

---

## 步骤 7：TODOS.md 清理

这是补充 `/ship` 步骤 5.5 的第二轮。读取 `review/TODOS-format.md`（如果可用）获取规范 TODO 项目格式。

如果 TODOS.md 不存在，跳过此步骤。

1. **尚未标记的已完成项目：** 将差异与开放的 TODO 项目交叉引用。如果某个 TODO 明显被此分支中的更改完成，将其移至已完成部分，并添加 `**完成于：** vX.Y.Z.W (YYYY-MM-DD)`。要保守 — 仅标记差异中有明确证据的项目。

2. **需要描述更新的项目：** 如果某个 TODO 引用了被显著更改的文件或组件，其描述可能已过时。使用 AskUserQuestion 确认 TODO 应该更新、完成还是保持原样。

3. **新的延期工作：** 检查差异中的 `TODO`、`FIXME`、`HACK` 和 `XXX` 注释。对于每个代表有意义延期工作（不是微不足道的内联注释）的注释，使用 AskUserQuestion 询问是否应该捕获到 TODOS.md 中。

---

## 步骤 8：VERSION 升级询问

**关键 — 永不在不询问的情况下升级 VERSION。**

1. **如果 VERSION 不存在：** 静默跳过。

2. 检查 VERSION 在此分支上是否已被修改：

```bash
git diff <base>...HEAD -- VERSION
```

3. **如果 VERSION 未被升级：** 使用 AskUserQuestion：
   - 建议：选择 C（跳过）因为仅文档更改很少需要版本升级
   - A) 升级 PATCH (X.Y.Z+1) — 如果文档更改与代码更改一起发布
   - B) 升级 MINOR (X.Y+1.0) — 如果这是一个重要的独立发布
   - C) 跳过 — 不需要版本升级

4. **如果 VERSION 已被升级：** 不要静默跳过。相反，检查升级是否仍覆盖此分支上的全部更改范围：

   a. 读取当前 VERSION 的 CHANGELOG 条目。它描述了什么功能？
   b. 读取完整差异（`git diff <base>...HEAD --stat` 和 `git diff <base>...HEAD --name-only`）。
      是否有重大更改（新功能、新技能、新命令、重大重构）未在当前版本的 CHANGELOG 条目中提及？
   c. **如果 CHANGELOG 条目覆盖所有内容：** 跳过 — 输出"VERSION：已升级至 vX.Y.Z，覆盖所有更改。"
   d. **如果有重大未覆盖的更改：** 使用 AskUserQuestion 解释当前版本覆盖什么与新内容是什么，并询问：
      - 建议：选择 A 因为新更改值得拥有自己的版本
      - A) 升级到下一个 patch (X.Y.Z+1) — 给新更改自己的版本
      - B) 保持当前版本 — 将新更改添加到现有 CHANGELOG 条目
      - C) 跳过 — 保持版本原样，稍后处理

   关键洞察：为"功能 A"设置的 VERSION 升级不应静默吸收"功能 B"，如果功能 B 足够重要，值得拥有自己的版本条目。

---

## 步骤 9：提交与输出

**首先检查是否为空：** 运行 `git status`（永不使用 `-uall`）。如果任何先前步骤未修改文档文件，输出"所有文档都是最新的。"并退出而不提交。

**提交：**

1. 按名称暂存修改的文档文件（永不 `git add -A` 或 `git add .`）。
2. 创建单个提交：

```bash
git commit -m "$(cat <<'EOF'
docs: 更新项目文档至 vX.Y.Z.W

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
EOF
)"
```

3. 推送到当前分支：

```bash
git push
```

**PR 正文更新（幂等、竞态安全）：**

1. 将现有 PR 正文读取到 PID 唯一的临时文件：

```bash
gh pr view --json body -q .body > /tmp/openclaw-pr-body-$$.md
```

2. 如果临时文件已包含 `## 文档` 部分，用更新内容替换该部分。如果不包含，在末尾追加 `## 文档` 部分。

3. 文档部分应包含**文档差异预览** — 对于每个修改的文件，描述具体更改了什么（例如，"README.md：向技能表添加了 /document-release，将技能计数从 9 更新为 10"）。

4. 将更新的正文写回：

```bash
gh pr edit --body-file /tmp/openclaw-pr-body-$$.md
```

5. 清理临时文件：

```bash
rm -f /tmp/openclaw-pr-body-$$.md
```

6. 如果 `gh pr view` 失败（PR 不存在）：跳过并显示消息"未找到 PR — 跳过正文更新。"
7. 如果 `gh pr edit` 失败：警告"无法更新 PR 正文 — 文档更改已在提交中。"并继续。

**结构化文档健康摘要（最终输出）：**

输出可扫描的摘要，显示每个文档文件的状态：

```
文档健康：
  README.md       [状态] ([详情])
  ARCHITECTURE.md [状态] ([详情])
  CONTRIBUTING.md [状态] ([详情])
  CHANGELOG.md    [状态] ([详情])
  TODOS.md        [状态] ([详情])
  VERSION         [状态] ([详情])
```

其中状态是以下之一：
- 已更新 — 附带更改内容描述
- 当前 — 无需更改
- 措辞润色 — 调整了措辞
- 未升级 — 用户选择跳过
- 已升级 — 版本由 /ship 设置
- 已跳过 — 文件不存在

---

## 重要规则

- **编辑前先读取。** 始终在修改前读取文件的完整内容。
- **永不覆盖 CHANGELOG。** 仅润色措辞。永不删除、替换或重新生成条目。
- **永不静默升级 VERSION。** 始终询问。即使已升级，也要检查是否覆盖全部更改范围。
- **明确更改了什么。** 每个编辑获得一行摘要。
- **通用启发式，非项目特定。** 审计检查适用于任何仓库。
- **可发现性很重要。** 每个文档文件应该可以从 README 或 CLAUDE.md 访问。
- **措辞：友好、面向用户、不晦涩。** 像向没看过代码的聪明人解释一样编写。
